services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: ledger_match
      POSTGRES_USER: ledger
      POSTGRES_PASSWORD: ledger
    ports:
      - "5432:5432"
    volumes:
      - ledger-match-data:/var/lib/postgresql/data
volumes:
  ledger-match-data: